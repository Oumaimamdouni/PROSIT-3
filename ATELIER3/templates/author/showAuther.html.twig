<table>
 <tr>
  <td>ID</td>
  <td>Picture</td>
  <td>Username</td>
  <td>Email</td>
  <td>Number of books</td>
  </tr>

  {% for authors in authorslist%}
  {% if id == authors.id %}
    <tr>
        <td> {{authors.id}} </td> 
        <td>  <img src="{{authors.picture}}"> </td>
        <td> {{authors.username|upper}}</td>
        <td> {{authors.email}}</td>
        <td> {{authors.nb_books}}</td>
        </tr>

    {%endif%}
    {%endfor%}
    </table>